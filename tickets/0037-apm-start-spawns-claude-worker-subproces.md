+++
id = 37
title = "apm start spawns claude worker subprocess"
state = "ammend"
priority = 0
effort = 6
risk = 3
author = "claude-0327-1854-10aa"
branch = "ticket/0037-apm-start-spawns-claude-worker-subproces"
created_at = "2026-03-28T02:28:59.787975Z"
updated_at = "2026-03-28T18:20:36.730435Z"
+++

## Spec

### Problem

`apm start <id>` currently provisions a worktree, claims the ticket, and prints
the worktree path. The supervisor agent is then responsible for doing the actual
implementation work — either inline or by spawning subagents via the Agent tool.

This creates several problems:
- Subagents spawned via the Agent tool run in the supervisor's process context, not in the worktree
- Every Bash call from a subagent requires individual user approval (permission noise)
- Compound shell commands (`cd $wt && cargo test`) trigger hardcoded security warnings that bypass the allow list
- Multiple parallel tickets compete for the supervisor's attention

The right model: `apm start <id>` provisions the worktree AND launches a `claude`
subprocess inside it, then **returns immediately**. The supervisor gets control back
and can start additional tickets or respond to the user. Each worker runs fully
autonomously in its own directory with its own permission context and is responsible
for committing, pushing, and opening a PR when done.

### Acceptance criteria

- [ ] `apm start <id>` provisions the worktree, launches a `claude` worker subprocess in the background, and returns immediately (non-blocking)
- [ ] The worker is launched with `--dangerously-skip-permissions` so it runs autonomously without prompting
- [ ] The worker is launched with `--print` (non-interactive) and receives `apm.worker.md` as system prompt
- [ ] The ticket content (`apm show <id>`) is passed as the initial user message to the worker
- [ ] Worker stdout+stderr are captured to `<worktree>/.apm-worker.log`; `apm start` prints the log path and worker PID before returning
- [ ] The worker is responsible for: committing changes, pushing the branch, opening a PR targeting `main`, and transitioning the ticket to `implemented` or `blocked`
- [ ] `apm start --no-spawn <id>` provides the previous behavior (provision worktree, print path, no subprocess)
- [ ] `apm init` creates `apm.worker.md` alongside `apm.agents.md`
- [ ] `apm.worker.md` is focused on worker-only concerns: implementing a ticket, committing, pushing, creating a PR, transitioning state, and the blocked protocol
- [ ] `apm.agents.md` (supervisor) is updated to describe the new workflow: run `apm start <id>` per ticket; it returns immediately; the worker handles everything from there
- [ ] The `APM_AGENT_NAME` for the worker is generated by `apm start` and passed as an environment variable to the subprocess

### Out of scope

- Monitoring or tailing worker logs via an `apm` command
- Killing or restarting running workers
- Worker health checks or timeout enforcement
- SDK-based worker orchestration (claude CLI is the implementation target)
- Multi-turn worker conversations (one prompt → worker runs to completion)

### Amendment requests

- [x] Clarified blocking vs non-blocking: `apm start` is non-blocking — it
  launches the worker in the background and returns immediately so the supervisor
  can start more tickets or respond to the user. The worker owns push and PR
  creation. The previous spec contradicted itself (Problem said "returns
  immediately", acceptance criteria said "blocks"); resolved in favor of
  non-blocking.

### Approach

**`apm/src/cmd/start.rs`**: After provisioning the worktree and claiming the ticket,
check if `--no-spawn` is set. If not:

1. Generate `APM_AGENT_NAME` as `claude-MMDD-HHMM-XXXX`
2. Read `apm.worker.md` from the repo root
3. Get ticket content from the ticket struct already loaded
4. Build the claude command with `--dangerously-skip-permissions`, `--print`,
   `--system <worker_md>`, and the ticket content as the user message
5. Redirect worker stdout+stderr to `<worktree>/.apm-worker.log`
6. Spawn the process detached (not waited on); print the PID and log path; return

No post-exit state inspection in `apm start` — the worker handles state transitions
and push+PR directly.

**`apm/src/cmd/init.rs`**: Create `apm.worker.md` during `apm init` using `include_str!`
on a new `apm.worker.md` template file in the repo.

**`apm.worker.md` template** (worker-specific instructions):
- Identity: set `APM_AGENT_NAME` from the environment variable provided by `apm start`
- Startup: `apm show <id>` to read the full spec (the ticket number is in the user message)
- Implementation: code changes, `cargo test --workspace`, commit all changes to the branch
- Push the branch: `git push origin <branch>`
- Open a PR: `gh pr create --base main ...`
- Transition state: `apm state <id> implemented`
- Blocked protocol: write questions to `### Open questions`, commit, push branch,
  run `apm state <id> blocked` — supervisor will read the questions from the branch

**`apm.agents.md` update** (supervisor-specific):
- Streamline to supervisor concerns only: spec review, unblocking, PR review
- Replace implementation workflow with: "Run `apm start <id>` — returns immediately;
  the worker runs in the background, commits, pushes, and opens the PR autonomously"
- Supervisor no longer needs to push branches, create PRs, or wait for workers

## History

| When | From | To | By |
|------|------|----|----|
| 2026-03-28T02:28Z | — | new | claude-0327-1854-10aa |
| 2026-03-28T02:31Z | new | specd | claude-0327-1854-10aa |
| 2026-03-28T18:20Z | specd | ammend | apm |
